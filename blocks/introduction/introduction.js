/**
 * loads and decorates the introduction block
 * @param {Element} block The introduction block element
 */
export default async function decorate(/* block */) {
  // Simple block - heading and body text are styled via CSS
  // No structural transformation needed
}
